type SomeObject = {
  id: number;
  name: string;
  lastname: string;
};

const removeDuplicatesAdvanced = (arr: SomeObject[]): SomeObject[] => {
  return [];
};

export default removeDuplicatesAdvanced;
